// modules and requires
// the code bellow implies the file in the people have some sort of array and 
//execute here in the second directory

const req = require('./people');

console.log(req.people, req.ages);